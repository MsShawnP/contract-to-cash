# Lailara LLC — Contract to Cash engagement config (DEMO)
# Canonical Cinderhaven identity; demo: true so the deploy guard ignores it.
# The deployed narrative renders committed frontend JSON — this config exercises
# client_mode.py on a demo-shaped invoice ledger under client-data/ (gitignored).
client:
  name: "Cinderhaven Provisions (demo)"
engagement:
  id: "CINDERHAVEN-DEMO"
as_of_date: "2026-01-02"
prepared_by: "Lailara LLC"
demo: true
inputs:
  input: "client-data/invoices.csv"
columns:
  invoice_id: "invoice_id"
  retailer: "retailer"
  invoice_date: "invoice_date"
  invoice_amount: "invoice_amount"
  amount_received: "amount_received"
  payment_date: "payment_date"
  deduction_amount: "deduction_amount"
thresholds:
  fail_on_missing_required_column: true
  fail_on_duplicate_keys: true
